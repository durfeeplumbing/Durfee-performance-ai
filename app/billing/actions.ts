'use server';
import { revalidatePath } from 'next/cache';import { createSupabaseServerClient } from '@/lib/supabase/server';import { requireCurrentUser } from '@/lib/session';
const finance=['owner','manager','accounting'];
export async function createInvoice(formData:FormData){const user=await requireCurrentUser();if(!finance.includes(user.role))throw new Error('Not authorized');const jobId=String(formData.get('job_id')??'');const supabase=await createSupabaseServerClient();const {data:job}=await supabase.from('jobs').select('revenue,material_cost,labor_cost,allocated_overhead,status').eq('id',jobId).single();if(!job)throw new Error('Job not found');const total=Number(formData.get('total')??job.revenue??0);if(total<=0)throw new Error('Invoice total required');const cost=Number(job.material_cost??0)+Number(job.labor_cost??0)+Number(job.allocated_overhead??0),gp=((total-cost)/total)*100;if(gp<50&&user.role!=='owner')throw new Error('Invoice below 50% GP requires owner approval');const {error}=await supabase.from('invoices').insert({job_id:jobId,status:'open',subtotal:total,tax:0,total});if(error)throw new Error('Invoice could not be created');await supabase.from('jobs').update({revenue:total}).eq('id',jobId);revalidatePath('/billing');revalidatePath('/dashboard');revalidatePath('/jobs');}
export async function recordPayment(formData:FormData){const user=await requireCurrentUser();if(!finance.includes(user.role))throw new Error('Not authorized');const invoiceId=String(formData.get('invoice_id')??''),amount=Number(formData.get('amount')??0),method=String(formData.get('method')??'').trim();if(amount<=0||!method)throw new Error('Valid payment required');const supabase=await createSupabaseServerClient();const {data:invoice}=await supabase.from('invoices').select('id,job_id,total').eq('id',invoiceId).single();if(!invoice)throw new Error('Invoice not found');const {error}=await supabase.from('payments').insert({job_id:invoice.job_id,invoice_id:invoiceId,amount,method,reference:String(formData.get('reference')??'').trim()||null});if(error)throw new Error('Payment could not be recorded');const {data:payments}=await supabase.from('payments').select('amount').eq('invoice_id',invoiceId);const paid=(payments??[]).reduce((s,p)=>s+Number(p.amount),0);if(paid>=Number(invoice.total))await supabase.from('invoices').update({status:'paid'}).eq('id',invoiceId);revalidatePath('/billing');}
export async function closeJob(formData:FormData){const user=await requireCurrentUser();if(!finance.includes(user.role))throw new Error('Not authorized');const jobId=String(formData.get('job_id')??'');const supabase=await createSupabaseServerClient();const [{data:job},{data:invoices},{data:payments}]=await Promise.all([supabase.from('jobs').select('revenue,material_cost,labor_cost,allocated_overhead').eq('id',jobId).single(),supabase.from('invoices').select('id,total,status').eq('job_id',jobId),supabase.from('payments').select('amount').eq('job_id',jobId)]);if(!job||!(invoices??[]).length)throw new Error('Invoice required');const billed=(invoices??[]).reduce((s,i)=>s+Number(i.total),0),paid=(payments??[]).reduce((s,p)=>s+Number(p.amount),0),cost=Number(job.material_cost??0)+Number(job.labor_cost??0)+Number(job.allocated_overhead??0),gp=billed>0?((billed-cost)/billed)*100:0;if(paid<billed)throw new Error('Outstanding balance prevents closeout');if(gp<50&&user.role!=='owner')throw new Error('Below-floor closeout requires owner');const {error}=await supabase.from('jobs').update({status:'closed',revenue:billed,completed_at:new Date().toISOString()}).eq('id',jobId);if(error)throw new Error('Job could not be closed');revalidatePath('/billing');revalidatePath('/dashboard');revalidatePath('/jobs');}
