import { createSupabaseServerClient } from '@/lib/supabase/server';import { requireCurrentUser } from '@/lib/session';import { createInvoice,recordPayment,closeJob } from './actions';const money=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'});
export default async function BillingPage(){const user=await requireCurrentUser();if(!['owner','manager','accounting'].includes(user.role))return <main style={{padding:32}}><h1>Billing</h1><p>You do not have access to financial closeout.</p></main>;const supabase=await createSupabaseServerClient();const [{data:jobs},{data:invoices,error}]=await Promise.all([supabase.from('jobs').select('id,status,revenue,material_cost,labor_cost,allocated_overhead,customers(name)').in('status',['completed','closed']).order('completed_at',{ascending:false}),supabase.from('invoices').select('id,job_id,status,total,jobs(customers(name)),payments(amount,method,received_at)').order('created_at',{ascending:false})]);return <main style={{fontFamily:'system-ui',maxWidth:1200,margin:'auto',padding:32}}><h1>Billing & Closeout</h1><p>Invoice, reconcile job cost, collect payment, and verify gross profit before financial closeout.</p><section style={{margin:'24px 0'}}><h2>Ready to Invoice</h2>{(jobs??[]).map((j:any)=>{const cost=Number(j.material_cost??0)+Number(j.labor_cost??0)+Number(j.allocated_overhead??0);return <article key={j.id} style={{border:'1px solid #ddd',borderRadius:14,padding:16,margin:'10px 0'}}><b>{j.customers?.name||j.id.slice(0,8)}</b><p>Recorded cost: {money.format(cost)} • Current revenue: {money.format(Number(j.revenue??0))}</p>{j.status!=='closed'&&<form action={createInvoice} style={{display:'flex',gap:8}}><input type="hidden" name="job_id" value={j.id}/><input name="total" type="number" min="0.01" step="0.01" defaultValue={Number(j.revenue??0)||undefined} placeholder="Invoice total" required/><button type="submit">Create Invoice</button></form>}</article>})}</section><section><h2>Invoices</h2>{error&&<p role="alert">Invoices could not be loaded.</p>}{(invoices??[]).map((i:any)=>{const paid=(i.payments??[]).reduce((s:number,p:any)=>s+Number(p.amount),0),balance=Math.max(0,Number(i.total)-paid);return <article key={i.id} style={{border:'1px solid #ddd',borderRadius:14,padding:16,margin:'10px 0'}}><b>{i.jobs?.customers?.name||'Job'} — {money.format(Number(i.total))}</b><p>Status: {i.status} • Paid: {money.format(paid)} • Balance: {money.format(balance)}</p>{balance>0&&<form action={recordPayment} style={{display:'flex',gap:8,flexWrap:'wrap'}}><input type="hidden" name="invoice_id" value={i.id}/><input name="amount" type="number" min="0.01" step="0.01" max={balance} defaultValue={balance} required/><select name="method" required><option value="cash">Cash</option><option value="check">Check</option><option value="card">Card</option><option value="ach">ACH</option><option value="financing">Financing</option></select><input name="reference" placeholder="Reference"/><button type="submit">Record Payment</button></form>}{balance===0&&<form action={closeJob}><input type="hidden" name="job_id" value={i.job_id}/><button type="submit">Financial Closeout</button></form>}</article>})}</section><aside style={{marginTop:28,border:'1px solid #ddd',borderRadius:16,padding:18}}><h2>Profit Guard</h2><p>Non-owner roles cannot create or close invoices below the 50% gross-profit floor. Jobs cannot financially close with an outstanding balance.</p></aside></main>}
