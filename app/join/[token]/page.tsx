import { createSupabaseServerClient } from '@/lib/supabase/server';import { joinEmployee } from './actions';
export default async function JoinPage({params,searchParams}:{params:Promise<{token:string}>,searchParams:Promise<{error?:string}>}){const {token}=await params,{error}=await searchParams;const supabase=await createSupabaseServerClient();const {data}=await supabase.rpc('get_employee_invite',{p_token:token});const invite=Array.isArray(data)?data[0]:data;if(!invite)return <main style={{fontFamily:'system-ui',maxWidth:560,margin:'80px auto',padding:32}}><h1>Invite unavailable</h1><p>This employee invite is expired, revoked, used, or invalid. Ask your manager for a new invite.</p></main>;return <main style={{fontFamily:'system-ui',maxWidth:560,margin:'80px auto',padding:32}}><h1>Join Durfee Performance AI</h1><p><b>{invite.employee_name}</b>, your account is approved for the <b>{invite.role}</b> role.</p>{error&&<p role="alert">Account setup could not be completed. Check the invite, email, and use a password of at least 10 characters.</p>}<form action={joinEmployee} style={{display:'grid',gap:12,marginTop:24}}><input type="hidden" name="token" value={token}/><label>Email<input name="email" type="email" defaultValue={invite.email} readOnly style={{display:'block',width:'100%'}}/></label><label>Create password<input name="password" type="password" minLength={10} required autoComplete="new-password" style={{display:'block',width:'100%'}}/></label><button type="submit">Create Employee Login</button></form><p><small>Invite expires {new Date(invite.expires_at).toLocaleString()}.</small></p></main>}
