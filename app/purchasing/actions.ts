'use server';
import { revalidatePath } from 'next/cache';import { createSupabaseServerClient } from '@/lib/supabase/server';import { requireCurrentUser } from '@/lib/session';
export async function createPurchaseOrder(formData:FormData){const user=await requireCurrentUser();if(!['owner','manager','accounting'].includes(user.role))throw new Error('Not authorized');const supplierId=String(formData.get('supplier_id')??''),customerRaw=String(formData.get('customer_id')??''),notes=String(formData.get('notes')??'').trim().slice(0,3000),expectedRaw=String(formData.get('expected_at')??'');if(!supplierId)throw new Error('Choose a supplier');const supabase=await createSupabaseServerClient();const {error}=await supabase.rpc('create_purchase_order',{p_supplier_id:supplierId,p_customer_id:customerRaw||null,p_notes:notes||null,p_expected_at:expectedRaw?new Date(expectedRaw).toISOString():null});if(error)throw new Error(error.message||'Purchase order could not be created');revalidatePath('/purchasing');}
