import { createSupabaseServerClient } from '@/lib/supabase/server';
import { requireCurrentUser } from '@/lib/session';
export default async function SchedulePage(){
  await requireCurrentUser();const supabase=await createSupabaseServerClient();const start=new Date();start.setHours(0,0,0,0);const end=new Date(start);end.setDate(end.getDate()+1);
  const {data,error}=await supabase.from('jobs').select('id,status,scheduled_start,scheduled_end,customers(name,service_address),users(name)').gte('scheduled_start',start.toISOString()).lt('scheduled_start',end.toISOString()).order('scheduled_start');const rows=error?[]:(data??[]);
  return <main style={{fontFamily:'system-ui',maxWidth:1250,margin:'auto',padding:32}}><h1>Schedule & Capacity</h1><p>Today’s live technician schedule. Assignment recommendations remain advisory.</p>{error&&<p role="alert">Schedule could not be loaded.</p>}<div style={{overflowX:'auto'}}><table style={{width:'100%',borderCollapse:'collapse',marginTop:24}}><thead><tr>{['Window','Technician','Customer','Service Address','Status'].map(x=><th key={x} style={{textAlign:'left',padding:12,borderBottom:'1px solid #ccc'}}>{x}</th>)}</tr></thead><tbody>{rows.map((j:any)=><tr key={j.id}><td style={{padding:12,borderBottom:'1px solid #eee'}}>{j.scheduled_start?new Date(j.scheduled_start).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'}):'—'}{j.scheduled_end?` – ${new Date(j.scheduled_end).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'})}`:''}</td><td style={{padding:12,borderBottom:'1px solid #eee'}}>{j.users?.name||'Unassigned'}</td><td style={{padding:12,borderBottom:'1px solid #eee'}}>{j.customers?.name||'—'}</td><td style={{padding:12,borderBottom:'1px solid #eee'}}>{j.customers?.service_address||'—'}</td><td style={{padding:12,borderBottom:'1px solid #eee'}}>{j.status}</td></tr>)}</tbody></table>{!rows.length&&!error&&<p style={{marginTop:24}}>Nothing is scheduled for today yet.</p>}</div></main>;
}
