import { createSupabaseServerClient } from '@/lib/supabase/server';import { requireCurrentUser } from '@/lib/session';import { updatePricingSettings } from './actions';
export default async function PricingSettingsPage(){const user=await requireCurrentUser();const supabase=await createSupabaseServerClient();const {data,error}=await supabase.from('company_pricing_settings').select('*').eq('id',true).single();if(user.role!=='owner')return <main style={{fontFamily:'system-ui',maxWidth:900,margin:'auto',padding:32}}><h1>Pricing Settings</h1><p>Owner access required.</p></main>;return <main style={{fontFamily:'system-ui',maxWidth:900,margin:'auto',padding:32}}><h1>Pricing Settings</h1><p>These are company-wide defaults used by the price book and estimate guardrails. Changes require owner authorization.</p>{error&&<p role="alert">Pricing settings could not be loaded.</p>}{data&&<form action={updatePricingSettings} style={{display:'grid',gap:14,border:'1px solid #ddd',borderRadius:18,padding:22}}><label>Labor rate per man-hour ($)<input name="labor_rate" type="number" min="1" step="1" defaultValue={Number(data.labor_rate)} style={{display:'block',width:'100%'}}/></label><label>Minimum gross profit (%)<input name="minimum_gp" type="number" min="0" max="99" step="0.1" defaultValue={Number(data.minimum_gp)} style={{display:'block',width:'100%'}}/></label><label>Default overhead per task ($)<input name="default_overhead" type="number" min="0" step="0.01" defaultValue={Number(data.default_overhead)} style={{display:'block',width:'100%'}}/></label><label>Membership discount (%)<input name="membership_discount_pct" type="number" min="0" max="99" step="0.1" defaultValue={Number(data.membership_discount_pct)} style={{display:'block',width:'100%'}}/></label><label><input name="allow_owner_below_floor" type="checkbox" defaultChecked={Boolean(data.allow_owner_below_floor)}/> Allow owner to approve pricing below the minimum GP floor</label><button type="submit">Save Company Pricing Settings</button><small>Last updated: {new Date(data.updated_at).toLocaleString()}</small></form>}</main>}
