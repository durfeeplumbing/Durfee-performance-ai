create or replace function public.has_permission_for_current_user(p_key text) returns boolean language sql stable security definer set search_path=public,private,pg_temp as $$ select private.has_permission(p_key) $$; revoke all on function public.has_permission_for_current_user(text) from public; grant execute on function public.has_permission_for_current_user(text) to authenticated;