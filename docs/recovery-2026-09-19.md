# FSM recovery — September 19, 2026

The production app has recovered from the failed September 6 build. Vercel deployment `dpl_22UdCg9dQ1jAkHj5c18E2P3KTgmc` for commit `03eb4adf91fe2626c0fe9142541120d68157a908` reached READY.

## Repairs

- Fixed malformed billing-page JSX and an unescaped dashboard apostrophe.
- Corrected three cache-refresh callbacks that passed array indexes to `revalidatePath`.
- Unified public route matching so employee invitation and customer approval links reach their token-validation pages.
- Added a dependency lockfile, exact package versions, generated-file exclusions, public-route regression tests, and a GitHub build-verification workflow.
- Resumed the paused existing Supabase project and verified its records were restored.
- Restored authenticated execution for seven private customer, billing, and team functions. Each function now independently checks the appropriate permission and active identity. Public RPCs remain security invoker; anonymous execution remains revoked. No role defaults or row policies were changed.

## Verification

- Production build, TypeScript check, and lint passed (8 pre-existing nonblocking lint warnings).
- Both public-route regression tests passed.
- Local production HTTP checks: login 200, dashboard redirects to login, invalid invitation shows the unavailable message, invalid estimate token returns 404, unauthenticated search returns 401.
- `tests/database-workflow.sql` passed 12 checks through the application's real public database functions: team setup, customer creation, scheduling, dispatch, field documentation and completion, follow-up rules, invoice creation, payment recording, and financial closeout. It also checks denied roles, unlinked users, incomplete work, and overpayment handling.
- The database test uses generated temporary identities and deliberately rolls back every business-data change. It requires a database administrator connection; it must never be exposed as an application endpoint.
- Database migration version: `20260919162900_restore_core_workflow_function_permissions`.
- The security-advisor categories/counts are unchanged after this repair. Existing advisories remain for token-based public functions, some other privileged functions, restricted tables, and password protection; this is not a full security sign-off.

## Remaining validation

Authenticated employee browser testing remains outstanding. Browser installation was unavailable in this execution environment, and no real employee password was requested or changed. The HTTP checks and database tests do not replace an authenticated UI pilot.

The pilot should enter a real verified address, schedule a job, assign a technician, capture field notes and labor, and create an invoice through the UI. Address-provider configuration, estimates and other untested modules, external integrations, and the requested portal custom domain still need separate verification. ServiceTitan sync was not run; the repository README currently defers that integration.
