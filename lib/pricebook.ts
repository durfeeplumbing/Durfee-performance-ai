export type PriceInput={materialCost:number;materialMarkupPercent?:number;laborHours:number;laborRate:number;overhead:number;targetGp:number};
export function buildPrice(i:PriceInput){if(i.targetGp<0||i.targetGp>=100)throw new Error('targetGp must be >= 0 and < 100');const materialSell=i.materialCost*(1+(i.materialMarkupPercent??0)/100);const labor=i.laborHours*i.laborRate;const loadedCost=materialSell+labor+i.overhead;const price=loadedCost/(1-i.targetGp/100);return{materialSell,labor,loadedCost,price,grossProfit:price-loadedCost,grossProfitPercent:i.targetGp};}
export function optionPrices(i:PriceInput){const base=buildPrice(i);return{Good:base.price,Better:base.price*1.12,Best:base.price*1.25};}
export type ActualJob={estimatedHours:number;actualHours:number;currentPrice:number;actualCost:number};
export function learningSignal(j:ActualJob,targetGp=50){const timeVariance=j.actualHours-j.estimatedHours;const actualGp=j.currentPrice?((j.currentPrice-j.actualCost)/j.currentPrice)*100:0;return{timeVariance,actualGp,needsReview:Math.abs(timeVariance)>=1||actualGp<targetGp,recommendation:actualGp<targetGp?'Review labor allowance, material cost, overhead, or price. Authorized approval is required before changing the live price book.':Math.abs(timeVariance)>=1?'Review estimated labor hours against completed-job evidence.':'No pricing adjustment recommended.'};}
